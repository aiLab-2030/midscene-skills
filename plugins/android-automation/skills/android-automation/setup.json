{
  "setupComplete": false,
  "prerequisites": {
    "nodeJs": {
      "required": true,
      "installed": false,
      "description": "Node.js >= 18.19.0 (npx will auto-download @midscene/cli)",
      "setupGuide": {
        "verifyCommand": "node --version"
      }
    },
    "adb": {
      "required": true,
      "installed": false,
      "description": "Android Debug Bridge (ADB) installed and device connected",
      "setupGuide": {
        "verifyCommand": "adb devices",
        "instruction": "Install Android SDK Platform Tools or use: brew install android-platform-tools"
      }
    },
    "device": {
      "required": true,
      "installed": false,
      "description": "Android device connected via USB with USB debugging enabled",
      "setupGuide": {
        "instruction": "Enable Developer Options on your device, then enable USB Debugging. Connect via USB and authorize the computer."
      }
    },
    "apiKey": {
      "required": true,
      "configured": false,
      "description": "AI Model API key via environment variable",
      "setupGuide": {
        "documentationUrl": "https://midscenejs.com/zh/model-common-config.html",
        "envVariables": {
          "required": ["MIDSCENE_MODEL_API_KEY"],
          "optional": ["MIDSCENE_MODEL_BASE_URL", "MIDSCENE_MODEL_NAME"]
        },
        "quickSetup": "export MIDSCENE_MODEL_API_KEY=your-api-key"
      }
    }
  }
}
